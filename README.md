# Toggled

An open-source feature-flag management platform.

## Configuration

Copy the example file and edit local values. `config.yaml` is gitignored.

```bash
cp config.example.yaml config.yaml
```
